## 1. Ajuste do Timer no Pinia Store

- [x] 1.1 Atualizar a propriedade computada `formattedTime` no `src/stores/workout-store.ts` para calcular corretamente os minutos isolando o resto da hora com o operador `% 3600`.
- [x] 1.2 Verificar que o linter e o build compilam sem nenhum erro ou regressão.
